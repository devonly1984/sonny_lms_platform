interface EnrollButtonProps {
    courseId:string;
    isEnrolled:boolean;
}
const EnrollButton = ({ courseId, isEnrolled }: EnrollButtonProps) => {
  return <div>EnrollButton</div>;
};

export default EnrollButton